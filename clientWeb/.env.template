# Environment-Specific Configuration Template
# Copy this file to .local.env, .dev.env, or .prod.env
# Set the 'env' environment variable to 'local', 'dev', or 'prod'

# ===================================
# Database Configuration
# ===================================
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=your_db_user
DB_PWD=your_db_password
DB_NAME=reachme_db

# ===================================
# Example Usage:
# ===================================
# For LOCAL:  export env=local   (uses .local.env)
# For DEV:    export env=dev     (uses .dev.env)
# For PROD:   export env=prod    (uses .prod.env)
#
# If DB_PWD starts with special char like %, you can:
# 1. Set it in the .{env}.env file directly (git-ignored)
# 2. Or override with: export DB_PWD='%your-password'
